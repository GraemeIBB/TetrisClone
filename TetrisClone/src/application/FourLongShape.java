package application;

import javafx.scene.paint.Color;
import javafx.scene.shape.Rectangle;

public class FourLongShape extends Shape{

	FourLongShape(){
		Rectangle recTest = new Rectangle();
		recTest.setX(120);
		recTest.setY(0);
		recTest.setWidth(30);
		recTest.setHeight(120);
		recTest.setFill(Color.CRIMSON);
	}
}
